/**
 * ENUMS
 *
 * 2021-03-26 友
 */

const pkg = require('../../package.json');

module.exports = {

    /**
     * 專案設定
     */
    project: {

        /**
         * 標題
         */
        title: `${pkg.description} - v${pkg.version}`
    }

}
